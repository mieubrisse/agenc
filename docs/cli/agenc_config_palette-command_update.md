## agenc config palette-command update

Update a palette command

### Synopsis

Update a palette command in config.yml.

Works for both built-in and custom commands. For built-ins, creates or updates
the config override entry. At least one flag must be provided.

Example:
  agenc config palette-command update startMission --keybinding="C-n"
  agenc config palette-command update dotfiles --title="📁 Dotfiles" --keybinding="f"


```
agenc config palette-command update <name> [flags]
```

### Options

```
      --command string       full command to execute
      --description string   description shown alongside the title
  -h, --help                 help for update
      --keybinding string    tmux keybinding (e.g. "f" or "C-y")
      --title string         title shown in the palette picker
```

### SEE ALSO

* [agenc config palette-command](agenc_config_palette-command.md)	 - Manage palette commands

###### Auto generated by spf13/cobra on 9-Feb-2026
